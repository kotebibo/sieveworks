{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The worker loop only needs to invoke our own commands (eval_range, core_status); no filesystem or shell plugin surface is exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
